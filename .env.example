# ═══════════════════════════════════════════════════════════════════════
# Price-My-Car (AutoIntel) — Environment Template
# Copy to `.env` and fill in real values. NEVER commit `.env`.
# ═══════════════════════════════════════════════════════════════════════

# No application secrets are currently required — the app runs fully
# self-contained (auth users are created via the UI and stored in
# users_db.json). Placeholder for future needs:
# SOME_API_KEY=
